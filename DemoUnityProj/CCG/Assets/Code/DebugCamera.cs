using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CCG
{
    public class DebugCamera : MonoBehaviour
    {

        // Update is called once per frame
        void Update()
        {
            /*Debug.Log($"BL = {GetComponent<Camera>().ScreenToWorldPoint(Vector3.zero)}");
            Debug.Log($"TR = {GetComponent<Camera>().ScreenToWorldPoint(new Vector3(Screen.width, Screen.height, 0f))}");
            Debug.Log($"Screen size = {Screen.width} : {Screen.height}");*/
        }
    }
}
